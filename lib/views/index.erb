<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
    <title>minitrue example</title>
    <meta charset="utf-8">
    <meta name="color-scheme" content="light dark">
    <meta name="description" content="A quick intro to minitrue"> 
  </head>

  <body>
    <main class="container">
      <h1> minitrue example </h1>
      <hr>

      <h2> config.toml </h2>
      <table>
        <tr>
          <th> Key </th>
          <th> Value </th>
        </tr>

        <% toml["minitrue_example"].each do |k, v| %>
          <tr>
            <td><%= k %></td>
            <td><%= v %></td>
          </tr>
        <% end -%>
      </table>

      <h2> database.yml </h2>
      <table>
        <tr>
          <th> Key </th>
          <th> Value </th>
        </tr>

        <% yaml["development"].each do |k, v| %>
          <tr>
            <td><%= k %></td>
            <td><%= v %></td>
          </tr>
        <% end -%>
      </table>
    </main>
  </body>
</html>
